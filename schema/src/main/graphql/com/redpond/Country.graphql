query Countries($filter: CountryFilterInput) {
    countries(filter: $filter) {
        code
        name
    }
}

query Country($code: ID!) {
    country(code: $code) {
        code
        name
    }
}
